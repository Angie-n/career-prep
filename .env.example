# Copy to .env.local and fill in (never commit real values).
# Google Cloud → Credentials → OAuth 2.0 Client IDs → Web client
# Authorized JavaScript origin: http://localhost:5188
VITE_GOOGLE_CLIENT_ID=
